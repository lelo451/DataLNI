package com.lni.datalni;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring configuration root (component scanning + auto-configuration). This is the
 * Spring {@code @SpringBootApplication} class only — the JavaFX entry point lives in
 * {@link DataLniFxApp} and the process {@code main} in {@link Launcher}.
 */
@SpringBootApplication
public class DataLniApplication {
}
